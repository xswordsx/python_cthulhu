from cx_Freeze import setup, Executable

setup(name = "Python Cthulhu",
        version = "0.2",
        description = "I wish, I wish a nigga would",
        executables = [Executable("RPG-main.py")])
